const hostname = "skillupacademy.localhost:3006";
const isPlatform = hostname === 'coursespro.resultspro.ng' || 
                     hostname === 'localhost:3006' || 
                     hostname === 'coursespro.localhost:3006';
console.log("isPlatform:", isPlatform);
